# Handoff 1.2a — Migraciones + Modelos + Seeder de permisos

**Estado:** Completada ✅

## Objetivo alcanzado
Tablas `permissions` (45 permisos granulares formato `modulo.accion`) y `role_has_permissions` creadas. Modelos `Permission` y `Role` actualizado con relaciones y helpers. Seeder idempotente asigna permisos por rol.

## Tablas creadas
- `permissions` (id, clave)
- `role_has_permissions` (role_id FK, permission_id FK)

## Seeders ejecutados
- 45 permisos granulares (módulos Nivel 1 y 2 únicamente)
- Admin: 45 permisos | Supervisor: 28 | Empleado: 10 | Cliente: 0

## Archivos creados
- `database/migrations/2026_06_25_000001_create_permissions_table.php`
- `database/migrations/2026_06_25_000002_create_role_has_permissions_table.php`
- `app/Models/Permission.php`
- `database/seeders/PermissionSeeder.php`

## Archivos modificados
- `app/Models/Role.php` (relación `permissions()`, `tienePermiso()`, `sincronizarPermisos()`, constantes de rol)
- `database/seeders/DatabaseSeeder.php` (agregado `$this->call([PermissionSeeder::class])`)

## Decisiones tomadas
- Sin campo `descripcion` en `permissions` (fiel a `03_schemas.md`)
- División de 1.2 en 1.2a (backend) y 1.2b (middleware+vistas) por límite de 5 archivos
- Seeder usa `firstOrCreate` + `sync` (idempotente)
- Permisos solo de Nivel 1 y 2 (no adelantar niveles, regla 13)

## Próximo paso
Microfase 1.2b — Middleware `CheckPermission` + Vistas de gestión de roles

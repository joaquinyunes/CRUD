# Handoff 1.4a — Categorías (CRUD)

**Estado:** Completada ✅

## Objetivo alcanzado
Migración `categorias` ejecutada. CRUD completo con buscador/filtro por estado, eliminado lógico (desactivar), validación. Vista index con tabla y paginación. Vista form reutilizable para crear/editar. Enlace en sidebar.

## Tablas creadas
- `categorias` (id, nombre, descripcion nullable, estado boolean default true)

## Archivos creados
- `app/Models/Categoria.php`
- `app/Http/Controllers/CategoriaController.php`
- `routes/categorias.php`
- `resources/views/categorias/index.blade.php`
- `resources/views/categorias/form.blade.php`
- `database/migrations/2026_06_25_000003_create_categorias_table.php`

## Archivos modificados
- `routes/web.php` (require categorias.php)
- `resources/views/layouts/partials/sidebar.blade.php` (enlace a categorías)

## Decisiones tomadas
- Misma estructura que roles (1.2b): middleware `permiso:categorias.*` en cada ruta
- Eliminado lógico: `estado = false` en lugar de DELETE
- Los permisos `categorias.ver/crear/editar/eliminar` ya existían en PermissionSeeder desde 1.2a
- Vista `form.blade.php` reutilizable para create/edit (usa `$categoria->exists`)

## Próximo paso
Microfase 1.4b — Productos (CRUD completo, búsqueda, filtros, duplicar, subida de imagen)

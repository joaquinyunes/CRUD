# Handoff 1.1 — Setup + Autenticación

**Estado:** Completada ✅

## Objetivo alcanzado
Laravel 13.17 instalado con Breeze (Blade + Tailwind + Dark mode). MySQL 8.4 configurado y corriendo en localhost:3306. Login, logout, recuperar contraseña y verificación de email funcionales. Registro público eliminado (solo admins crean usuarios).

## Tablas creadas
- `users` (con `role_id` FK nullable, `two_factor_enabled`)
- `roles` (Administrador, Supervisor, Empleado, Cliente)
- `cache`, `jobs`, `sessions`, `password_reset_tokens`

## Seeders ejecutados
- 4 roles + usuario admin (`admin@sistema.local` / `admin123`)

## Archivos creados/modificados
### Creados
- `app/Models/Role.php`
- `database/migrations/0001_01_01_000003_create_roles_table.php`
- `database/seeders/DatabaseSeeder.php`

### Modificados
- `database/migrations/0001_01_01_000000_create_users_table.php`
- `app/Models/User.php` (relación `belongsTo(Role)`, casts)
- `routes/auth.php` (eliminadas rutas register)
- `.env` (DB: `sistema_administrativo`, APP_NAME)

### Eliminados
- `resources/views/auth/register.blade.php`

## Decisiones tomadas
- **Tailwind CSS** con Dark mode
- **Laravel Sanctum** para API futura
- **Sin auto-registro público**
- MySQL se inicia manualmente (no como servicio Windows)

## Próximo paso en ese momento
Microfase 1.2 — Roles y Permisos granulares
